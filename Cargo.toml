[package]
name = "sekejap"
version = "0.1.0"
edition = "2024"
description = "A graph-first, embedded multi-model database engine"
license = "MIT"
repository = "https://github.com/insanalamin/sekejap"
homepage = "https://sekejap.mecha.id"
documentation = "https://github.com/insanalamin/sekejap#readme"
keywords = ["database", "graph", "vector", "spatial", "multi-model"]
categories = ["database", "embedded", "data-structures"]

[dependencies]
anndists = "0.1.3"
arc-swap = "1.8.0"
bincode = "1.3"
crossbeam = "0.8.4"
dashmap = "6.1.0"
distances = { version = "1.8.0", optional = true }
half = { version = "2.4", optional = true }
hnsw_rs = { version = "0.3.3", optional = true }
log = "0.4.25"
rand = "0.9.2"
redb = "3.1.0"
rkyv = "0.8.14"
rstar = { version = "0.12.2", optional = true }
seahash = "4.1.0"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
tantivy = { version = "0.25.0", optional = true }
tempfile = "3.24.0"
thiserror = "2.0.18"
toml = "0.8.19"
uuid = "1.20.0"

[dev-dependencies]
criterion = "0.8.1"
rusqlite = "0.32"
zip = "2.2"

[features]
default = []
vector = ["dep:distances", "dep:half", "dep:hnsw_rs"]
spatial = ["dep:rstar"]
fulltext = ["dep:tantivy"]
all = ["vector", "spatial", "fulltext"]

[package.metadata.docs.rs]
all-features = true
